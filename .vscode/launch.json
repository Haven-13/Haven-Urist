{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "byond",
			"request": "launch",
			"name": "Launch DreamSeeker",
			"preLaunchTask": "Build All",
			"dmb": "${workspaceFolder}/${command:CurrentDMB}"
		},
		{
			"type": "byond",
			"request": "launch",
			"name": "Launch DreamSeeker - Example",
			"preLaunchTask": "Build All (Dev)",
			"dmb": "${workspaceFolder}/${command:CurrentDMB}",
			"env": {
				"UTRACY_BIND_ADDRESS": "127.0.0.1",
				"UTRACY_BIND_PORT": "3000",
			},
		},
		{
			"type": "byond",
			"request": "launch",
			"name": "Reparse DM",
			"preLaunchTask": "dm: reparse",
			"dmb": ""
		}
	]
}
