/obj/machinery/camera/network/pluto/command
	network = list(NETWORK_COMMAND)

/obj/machinery/camera/network/pluto/first_deck
	network = list(NETWORK_FIRST_DECK)

/obj/machinery/camera/network/pluto/second_deck
	network = list(NETWORK_SECOND_DECK)

/obj/machinery/camera/network/pluto/third_deck
	network = list(NETWORK_THIRD_DECK)

/obj/machinery/camera/network/pluto/engineering
	network = list(NETWORK_ENGINEERING)

/obj/machinery/camera/network/pluto/cargo
	network = list(NETWORK_CARGO)

/obj/machinery/camera/network/pluto/exploration
	network = list(NETWORK_EXPLORATION_SHUTTLE_PLUTO)

/obj/machinery/camera/network/pluto/courier
	network = list(NETWORK_COURIER_SHUTTLE_PLUTO)

/obj/machinery/camera/network/pluto/security
	network = list(NETWORK_SECURITY)

/obj/machinery/camera/network/pluto/prison
	network = list(NETWORK_PRISON)

// Motion
/obj/machinery/camera/motion/command
	network = list(NETWORK_COMMAND)
